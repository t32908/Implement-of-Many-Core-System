use Makefile to compile in 1  
  $ cd 1  
  $ make  
  $ ./lena_GB  
  output file name is lena_GB.bmp  
  
use CMakelists to compile in 2  
  $ cd 2  
  $ cd build  
  $ make run  
  output file name is lena_GB.bmp  
