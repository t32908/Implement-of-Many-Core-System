use Makefile to compile in Gaussian_Blur_1
  $ cd Gaussian_Blur_1
  $ make
  $ ./lena_GB
  output file name is lena_GB.bmp

use CMakelists to compile in Gaussian_Blur_2
  $ cd Gaussian_Blur_2
  $ mkdir build
  $ cd build
  $ cmake ..
  $ make run
  output file name is lena_GB.bmp
